#Glyph recognition experiment

1. Run npm install in glyphgen folder
2. Run node glyphgen.js in glyphgen folder
3. There should now exist a folder called "glyphs" containing 32 png images in that folder
